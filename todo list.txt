Todo List
---------

// Ratio.js code
@todo Find out if cleanFormat() could be replaced with reduce()
@todo Change add "min" to the Ratio.min.js verison. So you know which version you're testing with.
@todo Ratio.prototype.equals(), decide if this will check for the same object or just only value. Should deepEquals( object ) == equals( object, true )?
@todo Consider wrapping Ratio inside a closure and getting rid of some redundant functions from the chain.
@todo Optimize Ratio.parse() and Ratio.clone()
	// Example Code for optimization.
	Ratio = {};
	Ratio.parseNumber = function (obj) {
		var arr = [+obj, 1], parts = obj.toString().split(/e/i), top, j;
		if (isNaN(obj)) {
			return arr;
		}	
		if( parts.length == 2 ){
				top = Ratio.parseNumber(parts[0]);
				j = Math.abs(+obj) < 1;
				arr[ j ? 0 : 1] = top[ j ? 0 : 1];
				arr[ j ? 1 : 0] =  +(top[ j ? 1 : 0 ] + "e" + Math.abs(+parts[1]));
		}else if ( ( parts = obj.toString().split(/\./) ) == 2 ) {
				arr[1] = Math.pow(10, parts[1].length);
				arr[0] = Math.abs(parts[0]) * arr[1] + (+parts[1]);
				arr[0] = (/\-/.test(parts[0])) ? -arr[0] : arr[0];
		}
		return arr;
	};

// environment
@todo Convert all files to UTF-8 with Unix EOL format.
@todo Create template for files that contains project version data that needs to be updated constantly.
@todo make automatic build for these.
	- Updated version information in Ratio.js
	- Minimize Ratio.js to Ratio.min.js
	- Updated documentation. Either for jsdocs or custom

// testing
@todo Add at least 5 user cases. a.add(4).toFraction() doesn't copy over the divSign.
	Ex: Ratio.parse(1/3).negate().add("-0.1").multiply(0xF3).divide(1,2).divide(1e-4).abs().toString()

// metrics
@todo Convert over to YUI profiler.
@todo Perform metrics on a growing matrix then graph the results.	

// jake
@todo Use the path module to form the file/folder paths.